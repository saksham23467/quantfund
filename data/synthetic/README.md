# Synthetic data

Place clearly labeled SYNTHETIC datasets here for local experiments.

The canonical deterministic fixture used by tests lives at:

`tests/fixtures/synthetic_bars.csv`

Never present synthetic data as real market data.
