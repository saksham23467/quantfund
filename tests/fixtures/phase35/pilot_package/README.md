# Phase 3.5 synthetic pilot package

Redistributable **synthetic** OHLCV for CI and pipeline verification.

- `source_grade = synthetic` → always `development_only`
- Real ISINs/symbols for identity tests; prices are fabricated
- Do not treat as research-grade market data
